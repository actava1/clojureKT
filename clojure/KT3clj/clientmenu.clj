(ns url-shortener.client
  (:require [clj-http.client :as client]))

(def base-url "http://localhost:3000")

(defn prompt [msg]
  (print msg)
  (flush)
  (read-line))

(defn create []
  ...
)

;; Аналогично show, update, delete как выше...

;; Полный код функций create, show, update, delete см. выше.

;; Ниже полный код меню:

(defn menu-loop []
  ;; Рекурсивный цикл меню
  ;; Возвращает nil при выходе
  
(letfn [(loop-menu []
         ;; Выводим меню и обрабатываем выбор пользователя:
         ;;
         ;;
         ;;
         ;;
         ;;
         ;;
         ;;
         ;;
         ;;
         ;;
         ;;
         ;;
         ;;
         ;;
         ;;
         ;;
         ;;
         ;;
         ;;
         ;;
         ;;
        
        ]
        
        )
        
)
